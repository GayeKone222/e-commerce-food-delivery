import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class Dimensions {
  // static double screenHeight(BuildContext context) =>
  //     MediaQuery.of(context).size.height;
  // static double screenWidth(BuildContext context) =>
  //     MediaQuery.of(context).size.width;

  static double get pageView => 320.h;
  static double get pageViewContainer => 220.h;
  static double get pageViewTextContainer => 120.h;

  static double get iconSize => 24.h;

  // static double pageView(BuildContext context) =>
  //   screenHeight(context) / 2.64;

  // static double pageViewContainer(BuildContext context) =>
  //     screenHeight(context) / 3.84;
  // static double pageViewTextContainer(BuildContext context) =>
  //     screenHeight(context) / 7.03;

  // static double height10(BuildContext context) => screenHeight(context) / 84.4;

  // static double height20(BuildContext context) => height10(context) * 2;

  // static double height15(BuildContext context) => screenHeight(context) / 56.27;

  // static double height30(BuildContext context) => height15(context) * 2;
}
